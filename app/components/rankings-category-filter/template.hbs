<select
  data-test-category-filter
  class="form-control"
  onchange={{action select value="target.value"}}
  ...attributes
>
  {{#each this.categories as |category|}}
    <option selected={{eq category @category}} value={{category}}>
      {{t (concat "category." category)}}
    </option>
  {{/each}}
</select>
